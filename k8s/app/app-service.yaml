apiVersion: v1
kind: Service

metadata:
  name: techbamboo-service
  namespace: techbamboo

spec:
  type: NodePort

  selector:
    app: techbamboo

  ports:
    - port: 3000
      targetPort: 3000
      nodePort: 30080